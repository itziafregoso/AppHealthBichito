<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <!-- DataLayer API -->
    <uses-permission android:name="com.google.android.gms.permission.WEARABLE_DATA_LAYER"/>

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:usesCleartextTraffic="true"
        android:name="HealthBichitoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthBichito">
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.provider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
    </provider>>

        <receiver
            android:name=".scheduling.ReinicioReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".scheduling.MedicacionReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="ALARM_MEDICACION"/>
            </intent-filter>
        </receiver>


        <receiver
            android:name=".scheduling.MarcarTomadoReceiver"
            android:exported="true" />

        <!-- Main Activity -->
        <activity
            android:name="com.example.healthbichito.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HealthBichito">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Servicio que recibe los datos del reloj -->
        <service
            android:name="com.example.healthbichito.data.wear.WearListener"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <!-- ✅ Acción CORRECTA -->
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />

                <!-- ✅ Filtro EXACTO del path (OBLIGATORIO) -->
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/health_data" />
            </intent-filter>
        </service>

    </application>

</manifest>
